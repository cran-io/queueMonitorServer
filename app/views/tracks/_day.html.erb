<div class="row">
	<div class="col-xs-6 no-padding">
		<% (0...12).each do |hour|%>
			<div class="col-xs-1 no-padding">
				<%= render partial: 'hour', locals: { hour: day[hour].to_a } %>
			</div>
		<%end%>
	</div>
	<div class="col-xs-6 no-padding"style="margin-left:-1px;">
		<% (12...24).each do |hour|%>
			<div class="col-xs-1 no-padding">
				<%= render partial: 'hour', locals: { hour: day[hour].to_a } %>
			</div>
		<%end%>
	</div>
</div>